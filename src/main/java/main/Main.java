package main;

import test.CollectData;

public class Main {
	/**
	 * Runs {@link CollectData#main(String[])}, generating and storing data.
	 * All the main settings are selected in-code.
	 * 
	 * @param args is ignored
	 */
	public static void main(String[] args) {
		CollectData.main(args);
	}
}
